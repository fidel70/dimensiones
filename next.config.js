/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Configuración necesaria para NextUI
  transpilePackages: ['@nextui-org/react'],
}

module.exports = nextConfig